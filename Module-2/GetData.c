// WAP to get the value from the user
#include<stdio.h>
int main()
{
    int a;// create the variable
    printf("Enter the value of a: ");// to get the value
    scanf("%d", &a);// to store the value
    printf("\nValue of the a is: %d", a);
    return 0;
}